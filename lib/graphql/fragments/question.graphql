#import "./tag.graphql"
#import "./user.graphql"

fragment HomeQuestion on Question {
  id
  slug
  title
  createdAt
  upvoteCount
  downvoteCount
  viewCount
  tags {
    items {
      tag {
        ...TagIdByLabel
      }
    }
  }
  owner {
    ...UserMinFragment
  }
}
